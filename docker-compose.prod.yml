services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
      target: prod
    restart: always
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.frontend
      target: prod
    restart: always
    ports:
      - "80:80"
  db:
    restart: always
