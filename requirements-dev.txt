# Dependencias para correr la suite de tests SIN levantar Docker.
#
# Es un subconjunto deliberado de requirements-fastapi.txt: los tests ejercitan lógica
# pura (relaciones, chunking, confinamiento de rutas, contratos de Architect/Solve) y
# todo lo pesado —torch, sentence-transformers, umap, hdbscan, playwright— se importa
# de forma perezosa dentro de las funciones que lo usan. Instalarlo acá agregaría
# ~2 GB y varios minutos a cada corrida de CI sin cubrir una línea más.
#
# Si algún test nuevo necesita esos modelos, no lo agregues acá: marcalo para que corra
# sólo dentro del contenedor, o el CI empieza a fallar por un motivo que no es el código.
#
#   python -m venv .venv
#   ./.venv/Scripts/python.exe -m pip install -r requirements-dev.txt   # Windows
#   .venv/bin/python -m pip install -r requirements-dev.txt             # Linux/macOS
#   python -m unittest discover -s tests

fastapi>=0.111.0
pydantic>=2.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
sqlalchemy[asyncio]>=2.0
asyncpg
psycopg2-binary
pgvector
anthropic>=0.28.0
httpx
numpy>=1.24.0
